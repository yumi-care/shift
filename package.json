{
  "scripts": {
    "build": "cd frontend && npm run build",
    "install": "npm install --legacy-peer-deps && cd frontend && npm install --legacy-peer-deps"
  },
  "devDependencies": {
    "puppeteer": "^24.41.0"
  }
}
